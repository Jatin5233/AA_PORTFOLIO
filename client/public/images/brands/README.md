# Brand logo assets

Add the approved brand logo files here using these exact names:

- `arvind-advertizing-logo.svg`
- `helix-esports-logo.svg`

PNG files can also be used if the paths in `client/src/pages/Home.tsx` are updated to match.
